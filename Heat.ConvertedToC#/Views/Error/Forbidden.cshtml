@{
    ViewBag.Title = "Forbidden";
    ViewBag.Description = "Mi dispiace, ma non hai accesso a questa pagina";
}

<div class="jumbotron text-center">
    <header>
        <h1><span aria-hidden="true" class="fa fa-ban text-danger"></span> @ViewBag.Title</h1>
    </header>
    <p>@ViewBag.Description</p>
    <a class="btn btn-primary btn-lg" href="@Url.Action("index", "home")"><span aria-hidden="true" class="fa fa-home"></span>Vai alla homepage</a>
</div>